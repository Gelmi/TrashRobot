# Trash Detectors

A small explorations of different machine learning models to recognize and classify trash.

All models were trained using the TACO Dataset.

To run each model, follow the instructions provided by the READme in each directory.
